{% extends 'base.html.twig' %}

{% block title %}Attack{% endblock %}

{% block body %}
    <h1>Attack</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attack.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ attack.Name }}</td>
            </tr>
            <tr>
                <th>Modifier</th>
                <td>{{ attack.Modifier }}</td>
            </tr>
            <tr>
                <th>Damage</th>
                <td>{{ attack.Damage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_attack_index') }}">back to list</a>

    <a href="{{ path('app_attack_edit', {'id': attack.id}) }}">edit</a>

    {{ include('attack/_delete_form.html.twig') }}
{% endblock %}
