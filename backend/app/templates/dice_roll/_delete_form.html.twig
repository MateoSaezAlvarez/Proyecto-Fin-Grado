<form method="post" action="{{ path('app_dice_roll_delete', {'id': dice_roll.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dice_roll.id) }}">
    <button class="btn">Delete</button>
</form>
